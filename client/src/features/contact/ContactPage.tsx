import { Typography } from "@mui/material";

export default function ContactPage() {
    return (
        <div>
            <Typography variant="h2">
                Contact
            </Typography>
        </div>
    )
}
